import { ScollprogressDirective } from './scollprogress.directive';

describe('ScollprogressDirective', () => {
  it('should create an instance', () => {
    const directive = new ScollprogressDirective();
    expect(directive).toBeTruthy();
  });
});
